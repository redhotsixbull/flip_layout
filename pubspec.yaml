name: flip_layout
description: Declarative layout & presence animations — enter/exit + FLIP reflow for any layout. A Framer Motion for Flutter.
version: 0.1.0
homepage: https://github.com/redhotsixbull/flip_layout
repository: https://github.com/redhotsixbull/flip_layout
issue_tracker: https://github.com/redhotsixbull/flip_layout/issues

topics:
  - ui
  - animation
  - layout
  - transitions

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
